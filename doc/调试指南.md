# 调试指南

> 本文档用于快速调试，了解本项目的基本功能

## 表结构准备

> 建表语句这里暂时仅提供mysql的语句，其它数据库自行使用大模型转换

```sql
CREATE TABLE `tb_user` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_name` varchar(100) DEFAULT NULL COMMENT '用户名',
  `login_name` varchar(100) DEFAULT NULL COMMENT '登录名',
  `login_pwd` varchar(100) DEFAULT NULL COMMENT '登录密码',
  `phone` varchar(50) DEFAULT NULL COMMENT '电话号码（密文）',
  `role_id` bigint DEFAULT NULL COMMENT '角色id',
  `create_time` datetime DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
)COMMENT='测试表-用户';

CREATE TABLE `tb_user_bak` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_name` varchar(100) DEFAULT NULL COMMENT '用户名',
  `login_name` varchar(100) DEFAULT NULL COMMENT '登录名',
  `login_pwd` varchar(100) DEFAULT NULL COMMENT '登录密码',
  `phone` varchar(50) DEFAULT NULL COMMENT '电话号码（明文）',
  `role_id` bigint DEFAULT NULL COMMENT '角色id',
  `create_time` datetime DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
)COMMENT='测试表-用户-备份表';
		
CREATE TABLE `tb_role` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `role_name` varchar(100) DEFAULT NULL COMMENT '角色名字（密文）',
  `role_desc` varchar(100) DEFAULT NULL COMMENT '角色描述',
  `create_time` datetime DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
)COMMENT='测试表-角色';
		
CREATE TABLE `tb_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `menu_name` varchar(100) DEFAULT NULL COMMENT '菜单名字',
  `path` varchar(100) DEFAULT NULL COMMENT '路径（密文）',
  `parent_id` bigint DEFAULT NULL COMMENT '父级id,第一级是0',
  `create_time` datetime DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
)COMMENT='测试表-菜单';

CREATE TABLE `sys_user` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `role_id` bigint(20) NOT NULL DEFAULT '0' COMMENT '角色ID',
  `login_name` varchar(50) NOT NULL COMMENT '登录名',
  `password` varchar(255) NOT NULL COMMENT '密码',
  `name` varchar(50) NOT NULL COMMENT '用户名',
  `mobile` varchar(20) DEFAULT NULL COMMENT '手机号',
  PRIMARY KEY (`id`)
) COMMENT='测试表-sys_user用户表2'
```

## 本地调试

### step1

>  自己找一个库，创建上列表结构

### step2

> 将项目pull下来，推荐拉取master分支的，这里是当前最稳定的版本

### step3

>  找到对应功能的测试用例，开测

- 字段加解密-db模式

  ```
  com.sangsang.test.SqlTest#testdbEncryptor
  ```

- 字段加解密-pojo模式

  ```
  com.sangsang.test.SqlTest#testPoJoEncryptor
  ```

- 异构数据库语法转换-mysql转达梦

  ```
  com.sangsang.test.TfMysql2DMTest#mysql2dmTransformation
  ```

- 异构数据库语法转换-oracle转mysql

  ```
  com.sangsang.test.TfOracle2MysqlTest#oracle2mysqlTransformation
  ```

- 数据权限隔离

  ```
  com.sangsang.test.IsolationTest#isolationTest
  ```

- 数据变更字段自动填充

  ```
  com.sangsang.test.FieldDefaultTest#fieldDefaultTest
  ```

### step4

> 将上面想要调的测试用例处理后的结果放数据库的客户端（比如：navicat，dbeaver）执行
>
> 体验一下改写后的sql是什么效果

### step5

> 动动小手给项目点个star